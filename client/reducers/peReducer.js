const initialState = {
  peStrike: 0,
  peHedgelegOffset: 0,
  peExitOffset: 0
};

const peReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default peReducer;
