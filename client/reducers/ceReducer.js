const initialState = {
  ceStrike: 0,
  ceHedgelegOffset: 0,
  ceExitOffset: 0
};

const ceReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default ceReducer;
