const initialState = {
  strikePrice: 0,
  stopLossPercent: 30,
};

const counterShortStraddleReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default counterShortStraddleReducer;
